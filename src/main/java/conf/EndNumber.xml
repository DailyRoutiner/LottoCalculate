<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ENDNUMBER">
	<resultMap type="EndNumber" id="endNumber">
		<result property="winningNumber" column="num" />
	</resultMap>
	
	<select id="selectEndNumber" resultMap="endNumber">
		select winning_number num from TOTAL_RESULT
	</select>
	
	<select id="selectTenEndNumber" resultMap="endNumber">
		select winning_number num from TOTAL_RESULT
		where 10 >= rownum
	</select>
	
</mapper>
